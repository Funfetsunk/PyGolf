# dev_config.py — development switches
#
# USE_GENERATED_COURSES
#   True  → the game always uses the built-in Python-generated courses,
#            ignoring any JSON files in data/courses/.
#            Use this while you are still building custom courses in the editor.
#   False → the game loads JSON courses from data/courses/ first (normal mode).
#            Flip this to False when your custom courses are complete.
#
USE_GENERATED_COURSES = True
